{
  "name": "everlightos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python -m http.server 8080 --directory site",
    "test": "echo \"No tests configured\"",
    "lint:md": "echo \"Markdown linting disabled\"",
    "precommit": "pre-commit run --all-files"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}
