# CDK Skeleton (Concept)

- `cdk.json` with context
- `lib/apm_stack.ts` defining API Gateway + Lambda + DynamoDB
- `bin/apm.ts` as entrypoint

Policies least-privilege; logging via CloudWatch; X-Ray optional.